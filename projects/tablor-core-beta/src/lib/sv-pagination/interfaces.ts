/**
 * Represents a processed search query.
 *
 * @property {string} query - The processed search query string.
 * @property {Array<string>} words - An array of strings representing individual words in the search query.
 */
export type ProcessedQuery = { query: string, words: Array<string> }
